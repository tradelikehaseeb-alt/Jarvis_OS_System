{
  "name": "@jarvis/browser-skill",
  "version": "0.0.0",
  "private": true,
  "description": "Browser skill — static browser actions (Phase 13)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@jarvis/skills-shared": "*",
    "electron": "42.3.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.8"
  }
}
