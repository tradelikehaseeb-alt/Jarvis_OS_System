# @jarvis/search-skill

Static **search** capability for Hermes (Phase 13). No network calls.

Bound to `hermes` via `@jarvis/agents-shared` skill pipeline.

```bash
npm run test --workspace=@jarvis/search-skill
```
