{
  "name": "@jarvis/memory-service",
  "version": "0.0.0",
  "private": true,
  "description": "Jarvis Memory Service — persistent memory owner (Phase 5 skeleton)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@jarvis/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  }
}
