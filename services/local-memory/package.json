{
  "name": "@jarvis/local-memory",
  "version": "0.0.0",
  "private": true,
  "description": "Jarvis local memory persistence — file-backed, SQLite-ready (Phase 62)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "electron": "42.3.2"
  }
}
