/** Hermes agent id — orchestrator handshake boundary (no agent package import). */
export const HERMES_AGENT_ID = "hermes" as const;

/** OpenClaw gateway agent id — orchestrator handshake boundary. */
export const OPENCLAW_AGENT_ID = "openclaw-gateway" as const;
