/**
 * Voice interaction modes (Phase 90).
 */
export type VoiceSessionMode = "continuous" | "push-to-talk" | "wake-word";

export const DEFAULT_VOICE_SESSION_MODE: VoiceSessionMode = "push-to-talk";
