{
  "name": "@jarvis/speech-service",
  "version": "0.0.0",
  "private": true,
  "description": "Speech transcript normalization — Roman Urdu + English (Phase 26)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  }
}
