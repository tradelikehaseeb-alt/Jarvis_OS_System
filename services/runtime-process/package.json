{
  "name": "@jarvis/runtime-process",
  "version": "0.0.0",
  "private": true,
  "description": "Jarvis runtime process manager — start, stop, monitor services (Phase 55)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  }
}
