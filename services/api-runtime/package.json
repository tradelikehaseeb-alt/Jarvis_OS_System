{
  "name": "@jarvis/api-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Jarvis HTTP API runtime — Desktop to Orchestrator server boundary (Phase 53)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@jarvis/orchestrator": "*",
    "@jarvis/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  }
}
