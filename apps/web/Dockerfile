# Jarvis OS Web — Phase 0 Dockerfile (production build in Phase 1)
FROM node:20-alpine AS base
WORKDIR /app

# Phase 1: multi-stage build with turbo prune
CMD ["node", "-e", "console.log('Jarvis web scaffold — run npm run build in Phase 1')"]
