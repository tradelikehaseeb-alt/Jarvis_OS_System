{
  "name": "@jarvis/openclaw",
  "version": "0.0.0",
  "private": true,
  "description": "OpenClaw agent + adapter boundary (Phase 16)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@jarvis/agents-shared": "*",
    "@jarvis/provider-registry": "*",
    "@jarvis/runtime-manager": "*",
    "@jarvis/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  }
}
