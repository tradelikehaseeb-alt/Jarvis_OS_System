{
  "name": "@jarvis/openclaw",
  "version": "0.0.0",
  "private": true,
  "description": "OpenClaw agent stub — execution gateway (Phase 10)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@jarvis/agents-shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  }
}
