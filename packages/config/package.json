{
  "name": "@jarvis/config",
  "version": "0.0.0",
  "private": true,
  "description": "Environment and runtime configuration types for Jarvis OS",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
