{
  "name": "@jarvis/provider-registry",
  "version": "0.0.0",
  "private": true,
  "description": "Pluggable provider selection for Hermes and OpenClaw adapters (Phase 17)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "electron": "42.3.2"
  }
}
